<div class="poll-form-wrapper">
  <%= simple_form_for(@poll) do |f| %>
    <% if @poll.errors.full_messages.length > 0 %>
      <div class="errors-wrapper">
        <h5>Your poll had the following errors:</h5>
        <ul>
          <% @poll.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <%= f.input :title, label: 'Poll Question', placeholder: 'Type your question here, options below' %>

    <% (1..4).each do |index| %>
      <%= f.simple_fields_for :options, f.object.options.build do |o| %>
        <span class="option-label"><%= "#{index}." %></span>
        <%= o.input :text, label: 'Poll Option', placeholder: 'Enter poll option' %>
      <% end %>
    <% end %>

    <%= f.button :submit %>
  <% end %>
</div>
